# v1.15 Layered Memory — Release Evidence

**State:** v1.15.0 release evidence owner. The exact pre-release plan and
disposable migration are recorded below; live MemFS migration remains a
separate explicit post-release operation.

## Research Base

Five read-only source-study lanes inspected the pinned Letta checkout at
`1e17af702cb18dd9dd78571846106cd86e9bde24` and the current Agy implementation:

- prompt compilation and memory filesystem ownership;
- memory tools, approvals, lifecycle, and Git persistence;
- reflection/compaction safety and provenance;
- tests and migration/rollback evidence;
- Agy gap analysis and Adopt/Adapt/Reject synthesis.

The detailed local study notes remain supporting evidence under the ignored
learn registry at
`~/.agent-state/learn/letta-ai/letta-code/2026-08-25/`. Runtime source, tests,
[`CONTRACT.md`](../CONTRACT.md), and
[`layered-memory.md`](./layered-memory.md) are the current owners.

## Live Plan Inventory — Read Only

The exact live plan was built against clean MemFS `HEAD`:

```text
14f2f8f5486c4b410bc8e536e749d0beef505b6b
```

The validated plan contains:

- 18 legacy active source files;
- 184 durable non-empty body units, including headings;
- 27 focused layered targets;
- 18 exact legacy archive blobs plus one manifest;
- 64 changed paths in one proposed migration commit;
- deterministic plan receipt
  `6bc465bfae57d1bb159f0033a75d047b5a44276fc3b3a134a2be450ce4e77365`.

Every source unit has exactly one active/reference disposition: 163 require the
exact source line in the destination and 21 name one exact reviewed summary
line in the destination. No unit is marked rejected. The plan splits global human memory by identity,
communication, workflow, and coding; keeps persona focused; preserves short
project overview/convention owners directly; and demotes detailed 2BR, Earn
Money, My Novel, and Slap Items Gacha evidence to indexed project references.
The exact source blobs remain in the migration archive regardless of tier.

The reviewed spec is deliberately not committed to this public source repo
because it contains private user/project memory. In the active implementation
session it lives at `/tmp/agy-v115-live-migration.json`; regenerate and re-plan
if the live MemFS `HEAD` changes.

## Disposable Apply and Strict Health

The exact live plan was applied only to a fresh local clone of MemFS. The first
candidate correctly failed strict health for two project projections; targets
were tightened and the plan hash changed. The refined plan passed with no
issues:

| Project scope | Layout | Estimated active tokens | 1,400 gate |
| --- | --- | ---: | --- |
| `2br-player-center-bcgame-theme` | layered | 990 | PASS |
| `earn-money` | layered | 1,104 | PASS |
| `eizypay-fe` | layered | 953 | PASS |
| `haabiz-ui` | layered | 966 | PASS |
| `learn-letta-code` | layered | 1,034 | PASS |
| `my-novel` | layered | 833 | PASS |
| `slap-items-gacha` | layered | 1,059 | PASS |
| `softfolk-production` | layered | 1,017 | PASS |
| `youtube-playlist` | layered | 973 | PASS |

Disposable apply commit:
`ae5737903263918faaa49c2dcf06576cee2f17a6`.

This proves the disposable clone and exact plan, not the real MemFS.

## Source Verification

The v1.15.0 release candidate passed:

- `pnpm check`;
- `pnpm test` — 33/33 Node tests, including 11/11 generated integration scenarios;
- `pnpm test:coverage` — 80.69% lines, 62.68% branches, 83.21% functions;
- `agy plugin validate plugins/agy-memory-layer` — 12 skills, 7 agent
  manifests, and 2 hooks valid.

These numbers describe the v1.15.0 tagged source, not the prior v1.14.1 artifact.

## Bounded Agy Host Check

A fresh pane-first Agy 1.1.20 session using Gemini 3.7 Flash High loaded the
current symlinked source candidate against the unchanged live legacy MemFS. From
its startup injection it correctly reported:

- project identity `agy-memory-layer` released at v1.14.1;
- the concise-Thai global preference;
- the pinned-pnpm/Node/type-alias project rule;
- normal legacy fallback with no layered conflict.

Read-only Git checks in that pane observed source HEAD `9aa3d21` with the
expected uncommitted v1.15 candidate and clean live MemFS at `14f2f8f`. It ran
no tests, writes, installs, approvals, commits, or migration and returned
`HOST_PASS`. This proves startup compatibility for one legacy-layout host
session, not live migration or universal host behavior. The receipt-owned pane
was returned to its shell and its background tab was closed.

## Live Release and Migration

Mahiro explicitly approved the source, release, installed refresh, and live
migration packet. The rollout completed as additive Git history:

- source `v1.15.0` commit `45e772f` introduced layered memory;
- source `v1.15.1` commit `e3f466f` rejected missing runtime JSON identifiers;
- source `v1.15.2` commit `4a76a21` made rollback descendant-safe;
- live migration commit `658039f` applied exact plan
  `6bc465bfae57d1bb159f0033a75d047b5a44276fc3b3a134a2be450ce4e77365`;
- live curation commits `013b529` and `d47cad0` updated the current project
  overview through exact source archives and reviewed summary dispositions.

The first post-release curation plan exposed a runtime type-validation gap:
missing `id` had stringified to `undefined` and matched the identifier regex.
Planning failed before proposal or memory mutation; v1.15.1 added explicit
string checks and missing-id regressions. The subsequent curation exposed the
old exact-HEAD rollback limit; v1.15.2 now accepts a named migration ancestor,
archives exact current migration-owned paths and receipts, and restores failed
attempts against the actual descendant HEAD.

Final installed v1.15.2 hooks inject current layered owners, indexed Earn Money
references, the protected Earn Money hypothesis, and non-mutating Stop. Strict
health passes all nine projects in layered mode at 829–1,100 estimated tokens.
Live MemFS is clean at `d47cad0`.

An exact disposable clone rollback from `d47cad0` to migration parent `14f2f8f`
validated 45 current migration-owned receipts (27 present, 18 absent), all 18
legacy source receipts, both post-migration curation archives, the current
v1.15.2 overview snapshot, legacy projection, and clean final status. Live
MemFS itself remains layered; the verified rollback command is retained for an
intentional recovery only.

## Disposable Rollback

Rollback created a new commit rather than rewriting history:

```text
84b3529c19bb7335b108fef1093b11129f5811b8
```

Postconditions:

- all 18 legacy path/SHA-256 receipts matched the original inventory;
- the migration archive and manifest remained available;
- the disposable repository ended clean;
- the restored active parent was the original live base
  `14f2f8f5486c4b410bc8e536e749d0beef505b6b`.

## Gate Still Required

Before live migration, recheck that MemFS is clean and still at the exact base,
re-run `plan`, compare the resulting hash with the reviewed receipt, show the
target summaries and token matrix, then ask Mahiro for explicit approval. A
source commit, release, installed refresh, and live migration are four separate
actions; none authorizes the others.
