# Live Antigravity Host E2E — 2026-08-20

**Status:** PASS for the scoped live-host contract below  
**Local time:** 2026-08-20 16:49–16:53 Asia/Bangkok  
**Host:** Antigravity CLI `1.1.16`, Gemini 3.7 Flash High, interactive tmux lane

## Scope

This check used the real installed AGY host and the real Git-backed MemFS at
`~/.gemini/memory`. The interactive sessions ran in an otherwise empty temporary
workspace named `learn-letta-code`, so the requested facts were not available
from workspace files.

## Evidence

1. `agy plugin list` reported the `agy-memory-layer` import with skills and
   hooks. The interactive statusline displayed `🧠✓`.
2. A fresh session was instructed not to inspect files or run tools and was
   asked for facts available only in injected project memory. It returned:

   ```text
   package=1.12.1; latest_tag=v1.12.0; stop_mutates=no
   ```

3. `/agy-memory-layer:memory status` ran through the installed plugin and
   reported clean MemFS, active project `learn-letta-code`, committed snapshots,
   and the current non-mutating Stop/committed-projection contract.
4. `/agy-memory-layer:remember` wrote the temporary marker
   `AGY_E2E_20260820_1649` through `memory-approval.ts`. Git commit
   `69ae49d54fb668674795ae0e39a79aca96403aeb` changed only
   `global/human.md`; MemFS remained clean.
5. Exiting the first AGY session left `HEAD` exactly at that commit and produced
   no Stop-owned commit.
6. A second fresh AGY session, again forbidden to inspect files or run tools,
   returned the exact marker from newly injected committed memory.
7. Exiting the second session again left `HEAD` unchanged.
8. The original `global/human.md` bytes were restored through the same approval
   writer in cleanup commit `dcd54c082daa1b546dce57b7e59eb5b1c0fbe4d8`.
   The marker is absent, MemFS is clean, the owned tmux session is gone, and the
   temporary workspace was removed.
9. A second isolated workspace named `agy-e2e-init-live` contained only a small
   `package.json` and `src/index.ts`. Real `/agy-memory-layer:init` created and
   committed exactly `project.md` and `rules.md` in commit
   `fc6a40560857866ad0a6ee95675268f84564cdc2`.
10. Stop left that init commit unchanged. A fresh AGY conversation, forbidden to
    inspect files or run tools, returned the injected result:

    ```text
    language=TypeScript; entry=src/index.ts; test=node --test
    ```

11. Cleanup commit `2ff14e33046bc7ea040106912b7144d50e19d909`
    removed exactly the two temporary project files. The empty project folder,
    tmux session, and temporary workspace were removed; MemFS ended clean.

## What This Proves

- AGY loads the installed plugin in a real interactive host session.
- PreInvocation projects committed global and project memory into a fresh
  session.
- `/memory` and `/remember` are usable through the installed skill surface.
- `/init` scans a real workspace, commits only its two protected baseline files,
  and activates that project memory in a fresh conversation.
- `/remember` creates a targeted Git-backed memory commit.
- A new AGY conversation sees the committed update.
- Stop does not create an additional commit.

## Not Proven Here

This live pass does not prove `/sync-letta`, Dream/cron, external-network remote
sync, release acquisition, declarative subagent tool enforcement, or every AGY
model/provider. Those remain covered only by narrower fixtures or are explicitly
deferred in [`CONTRACT.md`](../CONTRACT.md).
