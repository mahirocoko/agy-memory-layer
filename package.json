{
  "name": "agy-memory-layer",
  "version": "1.13.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "description": "Committed Git-backed MemFS, explicit Dream notes, codebase onboarding, and Memory Palace for Antigravity CLI",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsc --noEmit && node --experimental-strip-types --test --test-concurrency=1 tests/run-test-suite.ts tests/unit-coverage.test.ts",
    "test:coverage": "tsc --noEmit && node --experimental-strip-types --experimental-test-coverage --test --test-concurrency=1 tests/run-test-suite.ts tests/unit-coverage.test.ts",
    "check": "tsc --noEmit && biome check .",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "fix": "biome check --write .",
    "memory:health": "node --experimental-strip-types tools/memory-health.ts --strict"
  },
  "keywords": [
    "antigravity",
    "agy",
    "letta-code",
    "memory",
    "memfs",
    "ai-agent",
    "plugin"
  ],
  "author": "Mahiro",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@types/node": "26.2.0",
    "typescript": "5.8.2"
  }
}
